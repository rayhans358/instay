{
  "version":2,
  "buildCommand": "cd client && yarn install && ./node_modules/vite/bin/vite.js build",
  "outputDirectory": "client/dist",
  "builds": [
    { "src": "/api/(*.js)", 
      "use": "@vercel/vite" 
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}